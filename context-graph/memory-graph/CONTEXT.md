# Memory Graph

Memory Graph was the design name for what shipped as
[Sessions Graph](../sessions-graph/CONTEXT.md). It is not a separate package or
Context Graph component. `sessions-graph` was chosen because `memory-graph` was
unavailable on PyPI.

Use Sessions Graph terminology and documentation for Memory writes, recall,
ownership, provenance, and Session Reconciliation.
