# Memory Graph

Old design name for what shipped as [Sessions Graph](../sessions-graph/CONTEXT.md). Not a separate package or Context Graph component. Renamed to `sessions-graph`: `memory-graph` taken on PyPI.

For Memory writes, recall, ownership, provenance, Session Reconciliation: use Sessions Graph docs.
